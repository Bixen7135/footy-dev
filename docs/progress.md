# Progress

- 2026-03-28: Bootstrapped FOOTY v1 monorepo skeleton with FastAPI backend, worker, Next.js frontend (Bun), and baseline tests.
- 2026-03-28: Implemented v1 API groups (auth/catalog/cart/wishlist/orders/admin/events/recommendations/analytics/pages/jobs).
- 2026-03-28: Added SQLModel domain schema, ingestion/normalization/sync services, staleness and job orchestrator, worker scheduler.
- 2026-03-28: Added storefront/admin routes in Next.js, RHF+Zod forms, dwell/sendBeacon event tracking, recommendation slots.
- 2026-03-28: Added tests (`pytest`) for services and API import/order flows; all passing.
- 2026-03-29: Hardened auth/session lifecycle with `POST /auth/refresh`, configurable cookie policy, and FastAPI lifespan startup init.
- 2026-03-29: Hardened cart/checkout flow (no hardcoded checkout item, cart item ownership guard, stock/active checks, merge dedup by `(product_id, variant_id)`).
- 2026-03-29: Implemented hybrid admin media upload (`source_url` or multipart file) with MIME/size/path validation and backward-compatible behavior.
- 2026-03-29: Added local-dev smoke tooling (`scripts/run_local_smoke.ps1`), demo seeding, Playwright smoke tests, and updated runbooks/docs.
- 2026-03-29: Stabilized smoke suite determinism by fixing DB seed working directory and making stock e2e scenario self-contained via admin API setup.
- 2026-03-29: Completed frontend auth lifecycle hardening: auth-aware API retry (`401 -> /auth/refresh -> retry once`), session provider bootstrap via `/auth/me`, `/admin` + `/account` route guards, role-based login redirect, and header logout UX.
- 2026-03-29: Extended Playwright smoke with browser auth lifecycle scenario (`login -> me -> refresh -> me -> logout`) and verified full local smoke in one run.
- 2026-03-29: Added one-command local stack orchestration via `scripts/dev_stack.ps1` (`up/down/status/smoke`) and updated README/runbook; Docker kept optional.
- 2026-03-29: Added root Bun scripts (`dev:all`, `dev:all:status`, `dev:all:logs`, `dev:all:smoke`, `dev:all:down`) and improved dev stack UX by hiding spawned windows and adding live log tail command.
- 2026-03-29: Updated `dev:all` to run in the same terminal (`run` mode): starts stack, streams logs inline, and stops all services on `Ctrl+C`.
- 2026-03-29: Implemented `exports` bootstrap pipeline with CSV schema validation, idempotent upsert/mapping import, report persistence (`runbooks/reports`) and admin/job trigger `POST /admin/imports/bootstrap-exports`.
- 2026-03-29: Expanded recommendations to tech-spec candidate sources (tag affinity, recent orders, co-purchase, content/session/category similarity, trending/event-weighted) with hard filters and ML rerank fallback.
- 2026-03-29: Added online ranker artifacts layer (`LightGBM`), dataset builder + train jobs, admin endpoints (`/admin/recommendations/model`, `/admin/recommendations/train`), worker/manual bootstrap job support, and regression tests for new flows.
- 2026-03-30: Added explicit local reset+bootstrap command (`bun run dev:data:reset-bootstrap`) to rebuild `backend/footy.db` from `exports/` and fail fast on empty/placeholder catalog.
- 2026-03-30: Isolated `dev:all:smoke` into dedicated runner (`scripts/run_local_smoke_isolated.ps1`) with separate ports and `backend/footy-smoke.db`, plus guaranteed cleanup.
- 2026-03-30: Updated README/runbook/import docs to clarify local DB ownership (`backend/footy.db`) and new import/smoke workflow.
- 2026-03-30: Added catalog image support end-to-end (`ProductOut.image_url`), primary image selection from `product_images`, and storefront rendering on product cards/PDP/recommendation cards.
- 2026-03-30: Fixed CDN image URL normalization for exports (`.jpg/` -> `.jpg`), removing 404 image loads on storefront cards.
- 2026-03-30: Implemented PDP multi-image gallery: backend now returns `image_urls` for product detail and frontend renders thumbnail strip with switchable main image.
- 2026-03-30: Added strict working-DB protection: reset+bootstrap now runs dedicated `assert_no_test_data`, new `bun run dev:data:assert-clean`, smoke scripts hard-block shared DB usage, and backend API tests are isolated to a temporary sqlite DB.
- 2026-03-30: Implemented PDP size-first variant selection (`size_options` in product detail), sync signature-based product reuse + variant size upsert, and dedupe dry-run/apply jobs with safe relink/deactivate flow (links/variants/cart/wishlist), plus backend/API/e2e test coverage updates.
- 2026-03-30: Added admin backfill job `refresh_product_primary_images_job` to replace bad primary `/small/<number>.*` previews with validated `big/MAIN.*` images (HTTP 200 + image content-type), wired into `/admin/jobs/{job_name}/run`, with service/API tests and bootstrap runbook step.
